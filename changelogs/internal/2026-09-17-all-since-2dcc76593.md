# Internal Changelog — `all` branch since `2dcc76593`

- 范围：`2dcc76593`（2026-09-13）→ `all`（2026-09-18）；本地提交 **94 条**（另有 7 次合并），另含本次合并带入的上游 **4.28.0-beta.1 / 4.28.0 / 4.28.1**（60 条上游提交）。
- 期间合并 7 次：`f9488d514`（上游 `fce1bbc97`）与 6 条特性分支（MCP stdio 策略、更新源设置、搜索跳转、更新源字段清理、胶囊交互修复、会话导出/导入）。
- 同一功能的实现 / 迭代 / 修复归并为一条，括号内按时间顺序列出全部提交；上游部分**只列采纳的条目**。

### 新增

#### 聊天与输入框

- 每回合文件变更总结卡片：Agent Loop 收尾按文件聚合本轮净 diff，展示于最终回复下方，支持查看 diff / 在磁盘打开 / 单文件撤销。 (`0c98a33f6`, `99c7498bd`, `c92e1d3d1`, `35714ec7c`, `e9c48061e`, `c27a0b96e`, `e8fcdec24`, `0f1429f51`, `d5433ed74`, `dbc1e274b`, `e86dbec78`, `f12b7edcf`, `81b7430e0`, `12e91f37a`, `d63330987`, `7c15787a4`, `2ab44917a`)
- 会话右键菜单：重命名 / 归档 / 删除 / 标记为未读，打开会话即已读。 (`3d1064997`)
- 会话星标：右键菜单新增星标 / 取消星标，并在行首显示星标标记。 (`8cee6f6ba`)
- ChatUI 会话导出 / 导入：把 webchat 会话（消息流 + 附件 + 侧边线程 + LLM 上下文 + 偏好）打包为带 manifest 的 zip 导出，任意登录用户可导入到自己账户（暂存 + 预检 → id 重映射 → 单事务落库；解压只读 manifest 声明路径并限尺寸，跨版本包警告接受）；侧栏会话行 / 右键菜单导出入口与侧栏导入对话框（四语），后端 3 个路由。 (`ca8fb309d`, `7b282c57e`, `ada9df745`, `b890de19c`, `7b5ed6f26`, `aa9adec89`, `9186271e1`, `5d86b3d7d`, `1ce48da39`, `e5c5309d5`, `fcce974f3`, `0b3fcb25b`, `63d1c421c`, `24fd96ec0`, `01a886d09`)
- 交互选项折叠进 Agent 工作胶囊：已作答的 `ask_user_choice` 随工作轨迹折叠，展开时以复查模式渲染。 (`882170d9b`)
- 追加消息卡片拆为「追加」与「强制打断」：追加不打断运行、强制打断则停止运行再发送新消息。移除自动追加行为。 (`7209744e0`, `f5cf82385`, `8f37ab922`)
- 运行中且输入框为空时，Enter 确认最早的待追加消息（每条一次），Ctrl/Cmd+Enter 强制打断并重发；有文本时仍走正常发送。 (`9daa63c72`)
- 侧栏项目行新增项目内新建会话快捷按钮（带圈+号）。 (`31fd86b57`, `b0095db7a`)

#### Dashboard

- spcode 会话级 umo：`useSpcodeProjectStatus` 由模块级单例改为按 umo 分条并钉住活跃会话，新增 `useSpcodeSession` 上下文，21 文件 / 97 处读点改读会话身份，附护栏与跨会话回归测试。 (`f3cab9b20`, `a110996c6`, `920a79695`, `e7d23e523`, `c26acb2df`, `cd1abc579`, `b41c5c27e`, `95a62986c`, `9187f626e`, `13878b0a3`, `9ec9ad67d`, `f7ab6c07a`, `9d6006b64`, `08de98ca2`, `bb8d323fb`, `b9e7d4155`, `a38ac32b9`, `4d1bac04e`, `b8620d507`)
- 新建 worktree 的起点改为分支 / 标签下拉（默认当前分支），失败原因码接入白名单提示。 (`960ee7141`, `25a6fc89a`)
- 设置页新增 MCP stdio 命令策略（`mcp_settings.stdio_allowlist` / `stdio_denylist`）。 (`53d9ab3e4`)
- 设置页新增「更新源」配置：可改三个主下载源 URL（显式保存 / 恢复默认 / 逐字段校验 / 环境变量锁定提示），保存按 key 原子重写、无需重启即生效；新增 `GET/PUT /api/v1/updates/sources`，OpenAPI 与前端客户端同步。 (`db5113af4`)

#### 核心与工具

- 本地 shell 工具的 `yield_time_ms` 上限由 30s 提升至 120s。 (`d96609a74`)

### 修复

- 聊天视口按会话记忆并按消息索引恢复：切换会话不再串位，恢复不播滑动动画，打开时多次收敛等待后续增高，切到运行中会话重新跟随输出。 (`2d8ababca`, `8013c2b6e`, `6ed6d3ae8`, `e8264682b`, `c4b249a23`)
- 标记跳转改为落地后持续校正 + 滑行到位，并先等在途的历史加载落地。 (`75c57442a`, `fd2b59b82`, `d79a6fdd6`)
- 分支会话的滚动条标记点：继承消息按 `branch_info` 标注，折叠时隐藏。 (`7df4dc36d`)
- 工作胶囊与推理侧栏：回复之后的工作也收回胶囊（暂停回合不再常驻空的工具卡片）；侧栏改用全量块索引，折叠时不再打开错块或空面板。 (`ffa57ea62`, `5f7015378`)
- 修复 `ask_user_choice` 的结果被渲染成无名的「已使用 tool 工具」条目（交互事件落盘会提前终结累积器）：每回合共享抑制集、跨 flush 携带待回结果的调用，结果事件不再进入 SSE。 (`fa6d99c10`)
- 流式气泡在 id 落盘后不再消失（消息合并会把它当成过期副本丢弃，此前只有整页刷新能恢复）。 (`cad8df302`)
- 流式期间的吸底不再关闭「跟随输出」意图（自身滚动回声不再被当成用户滚动）。 (`2e46e2e4a`)
- 消息搜索跳转落到命中的内容：命中 Agent 工作轨迹时展开胶囊，命中思考文本时把关键词交给推理侧栏并滚动 / 高亮该条目（路由中的跳转参数随之清理）。 (`ff47d1acb`, `e26ed7670`)
- 移除 `update_config.json` 中已无调用点的更新源字段（上游 #9493 后失效的 `github_archive_url_template` / `github_release_api_url` 及其环境变量与 getter），并同步四语设置页文案。 (`e278cf98a`)
- 推理侧栏的思考文本字号下调一档。 (`e9bdeab2e`)

### 维护

- 修复三个漂移失败的聊天 spec（`DocumentManager.spec.ts` 缺 pinia 等）。 (`cc425c280`)
- 文件变更 diff / 撤销路由测试、import 修正与 mdi 子集刷新。 (`871011694`, `7c6d7ced7`)
- 会话作用域回归测试补强（跨会话 umo 不变量、指示器传参、覆盖率）。 (`a38ac32b9`, `95a62986c`, `4d1bac04e`)
- dev 回退警告与注释修正。 (`b9e7d4155`, `b8620d507`)
- 文档：会话导出设计、文件变更计划、会话级 umo 规格与实施计划。 (`4e4b7b737`, `a110996c6`, `920a79695`, `e7d23e523`, `bd291f3b9`)
- 会话导出 / 导入的实施计划文档与 API 客户端重生成（`openspec/openapi-v1.yaml`、`docs/public/openapi.json`、生成器产物）。 (`3806f712f`, `4e0f220c7`)

### 来自上游 4.28.0-beta.1 / 4.28.0 / 4.28.1（本次合并带入）

合并范围 `385e4b6bf`（上游 4.27.4）→ `fce1bbc97`（4.28.1）。仅列出被采纳的条目；🔧 = 改动后采纳（实现或文案按本地口径调整），◐ = 部分采纳，未标注 = 原样采纳。

#### 4.28.1

- 工作区代码预览改用 Shiki 高亮。 (#10023)
- 模型选择器提供商来源筛选与分组标题吸顶，配置对话框优化。 (#10084, #10007)
- 自适应输入图片处理（缩放 / 转格式 / 动图转拼图），CUA 沙箱保留静图尺寸并告警。 (#9703, #10071) — 🔧
- `/new` 与 `/reset` 统一为"开新会话、保留历史"，权限可在指令管理中配置。 (#10004) — 🔧
- 桌面端更新下载进度、精简打包字体与图表资源。 (#10050, #10034)
- 配置滑块健壮性。 (#10037)
- 统一模型提供商请求的 User-Agent 并包含版本号。 (#10082) — 🔧
- 事件清理后保留图片附件原图路径。 (#10042) — 🔧
- 修复通过 URL / 文件安装插件。 (#10053)
- 主动 Agent 的会话模型回退。 (#10028)
- skills-like 回退回复的流式送达。 (#10025) — 🔧
- 定时任务遵循会话上下文压缩设置、ERROR 结束时记录失败。 (#9992, #9987) — 🔧
- `api_base` 为空时的 OpenAI 接口解析。 (#10079)
- 网页搜索工具的字段 / 错误码 / 参数校验。 (#9979)
- 知识库 URL 清洗限流错开。 (#10015)
- 飞书私聊送达回退。 (#10018)
- QQ 官方 C2C 流式回复结束帧。 (#10069)
- Telegram 轮询与冲突日志降噪。 (#10027, #10046)
- 消息概要全体提及处理顺序。 (#9993)
- 当前对话删除行为与 WebChat 会话标题回退。 (#10003, #9983)
- 控制台日志批量渲染。 (#9988, #9990)
- 桌面端托管模式过期资源、PyPI 排除构建产物。 (#9898, #10036)

#### 4.28.0 与 4.28.0-beta.1

- WebUI 提供商 / 机器人 / 配置文件页重设计，日志、追踪、对话、数据合并为「数据与日志」，Agent 执行器内嵌到配置文件。 (#9820, #9825, #9821, #9883, #9846)
- 默认与本地 T2I 渲染重设计。 (#9803)
- 各平台适配器统一群组元数据增强。 (#9851)
- UMO 可读名称自动记录。 (#9909)
- 敏感值掩码 / 密码字段支持。 (#9824)
- 技能管理页批量删除。 (#9906)
- 新增 AnySearch 内置网页搜索服务商。 (#9767)
- 登录页密码管理器识别、提供商图标深色适配、未来任务 Agent 上下文、技能编辑器小屏适配、OpenAI 工具 schema 按名排序。 (#9830, #9647, #9818, #9873, #9798)
- 日语本地化（WebUI 与内置插件）。 (#9955) — 🔧
- ChatUI 代码高亮语言扩展、T2I 加入 Go 高亮。 (#9892) — 🔧
- 统计数据上报包含 Python 版本。 (#9973)
- 三平台单元测试 CI 与跨平台兼容。 (#9954) — ◐
- Anthropic / Gemini 缓存 token 用量统计。 (#9880, #9881) — 🔧
- 主动 Agent 遵循计算机运行时配置。 (#9971) — ◐
- 其余修复：钉钉扫码状态、飞书 at 命令触发、限流按 UMO 隔离、Anthropic Base URL 对齐、后台 Agent 应用 `max_agent_step`、飞书图文混合、移动端人格管理器、英文翻译补齐、提供商同步翻译命名空间、Windows 远端沙箱技能包路径、插件市场同名详情页、字典配置保留用户内容、工具重查保留上下文前缀、知识库分页选项、QQ 主动消息 Markdown、看门狗输出隔离。

### 合并期口径改动

- 历史窗口改为可选 + 游标分页，避免所有调用方被静默截断到 50 条；失败页暂停重试。 (`494696c20`)
- Shiki 受控语言集取本地与上游并集。 (`fbcfffab6`)
- 历史会话列表按上游默认列出 WebChat 会话，移除本地开关。 (`2f48a3937`)
- 修复 `MessageList.vue` 标签不配对、删除失效的 `MessageContentTransition.vue`，并重生成 mdi 子集（388 → 390）。 (`0702770f8`)
